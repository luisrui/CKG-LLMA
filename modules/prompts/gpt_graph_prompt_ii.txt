You are GPT-4. Your role is to verify the knowledge graph triples based on a recommendation task setting. KG Triples are in form of [head, relation, tail]. You need to find wrong triples and output in a single json file with 'add' and 'delete'.
We have triples of a knowledge graph:<<Triples>>. Titles of these items are:<<TITLE_NAMES>>. The triples are consist of item-item triples(I-I triples, two items share same attribute) and item-attribute triples(I-a triples). Based on your knowledge of these entities(including items, item attributes), use 'delete' on certain I-a triples without explanations if you find one's attribute is not related to itself and use 'add' if you identify A's attributes 'a' also associated with 'B', but there is no ('B', has xxx, 'a') in provided triples. I-I triples serves as connections between triples to help you find mistakes in I-a triples, and don't rectify these relations. Output the potential wrong triples in json file format. An example: Input: triples:{('lemon', 'has taste', 'sour'), ('ice cream', 'has taste', 'sour'), ('lemon', 'same taste', 'ice cream'),('cake', 'has taste', 'sweet'), ('ice cream', 'has ingredient', 'egg'), ('cake', 'has ingredient', 'milk')} Output:{'delete':[('ice cream', 'has taste', 'sour'), ('ice cream', 'has ingredient', 'egg')], 'add':[('ice cream', 'has taste', 'sweet'), ('ice cream', 'has ingredient', 'milk'), ('cake', 'has ingredient', 'egg')]} Explanation: ('lemon', 'same taste', 'ice cream') is wrong. Ice cream is not sour, but sweet. Milk is associated with ice cream, not egg. Cake has ingredient egg.